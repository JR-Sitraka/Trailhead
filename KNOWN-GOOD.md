# Known-Good State & Project Hard Rules — Trailhead

Referenced from `AGENTS.md`: required context on every implementation
task. Two sections, both append-only in spirit — entries get
corrected, not silently deleted.

**Genuinely empty as of 2026-07-20, and that's correct, not an
oversight.** No implementation has happened yet — nothing has actually
been run, so there are no real environment facts or incidents to
record. Inventing entries here before they're earned would violate
this file's own design intent (and `principles.md` rule 4's no-
invented-scope rule extends naturally to this file too). The first
entries should come from real friction hit during actual
implementation, starting with the security-critical `PREPROC-*` work
and the `pgvector` query-pattern check `testing.md` already flags as
high-priority.

## Known-good state

(none yet)

## Project hard rules

(none yet)
